<Project Sdk="Microsoft.NET.Sdk">

  <!-- AI 运行时单测：凭据库（假 cipher + DPAPI 实现）/ 原子写 /（后续）服务商配置、协议适配、会话存储。
       引用 Engine 仅为 TempArea.Resolve()（测试临时目录的唯一入口）；测试库一律临时目录、用完自清。 -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\LinkPocket.Ai\LinkPocket.Ai.csproj" />
    <ProjectReference Include="..\..\src\LinkPocket.Composition\LinkPocket.Composition.csproj" />
    <ProjectReference Include="..\..\src\LinkPocket.Engine\LinkPocket.Engine.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

</Project>
