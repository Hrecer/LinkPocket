# LinkPocket 依赖审计清单（2026-09-15，Material 3 迁移）

> 背景：UI 库从 MaterialDesignThemes（Material 2）整体迁移到 Material3.Wpf（Material 3 / Material You，
> dary1337/material3-dotnet，NuGet 0.9.0），借此对所有依赖做一次全面清查。

## 一、NuGet 包清单（迁移前 6 个 → 迁移后 5 个）

### LinkPocket.csproj（WPF 前端）

| 包 | 版本 | 决定 | 理由 |
|---|---|---|---|
| MaterialDesignThemes | 5.1.0 | ❌ **移除** | Material 2 规范，风格过时（9.7MB 是输出目录最大的 DLL）；由 Material3.Wpf 取代 |
| MaterialDesignColors | 5.2.0 | ❌ **移除** | MaterialDesignThemes 的配套色板包，随主包一起移除 |
| XamlFlair.Wpf | 1.2.13 | ❌ **移除** | **全项目 0 处引用**（审计确认），纯死重；连带移除其依赖 System.Reactive（1.4MB） |
| Material3.Wpf | 0.9.0 | ✅ **新增** | Material 3 运行时主题（单种子色生成全套角色画刷）+ M3 控件 + M3Icon |
| MdXaml | 1.27.0 | ✅ 保留 | 详情页 Markdown 渲染（`MarkdownScrollViewer`），有真实引用，无替代必要 |
| CommunityToolkit.Mvvm | 8.2.2 | ✅ 保留 | `AsyncRelayCommand` / `ObservableObject` 有真实引用 |

### LinkPocket.Core.csproj（后端）

| 包 | 版本 | 决定 | 理由 |
|---|---|---|---|
| Microsoft.EntityFrameworkCore.Sqlite | 8.0.10 | ✅ 保留 | 数据访问核心 |
| Microsoft.EntityFrameworkCore.Design | 8.0.10 | ❌ **移除** | 只在 `dotnet ef` 迁移时需要；项目有意不用迁移体系（HANDOFF 技术债），纯死重 |

## 二、为什么编译后会有那么多 DLL？

输出目录的 26~29 个 DLL 里，**只有 2 个是我们自己的代码**（LinkPocket.dll、LinkPocket.Core.dll），
其余全部是 NuGet 包的**传递依赖**，由 NuGet 自动复制，删不掉、也不需要删：

| DLL 组 | 数量 | 谁带进来的 | 说明 |
|---|---|---|---|
| EntityFrameworkCore(.Abstractions/.Relational/.Sqlite) | 4 | EF Core Sqlite | ORM 本体，必需 |
| Microsoft.Data.Sqlite + SQLitePCLRaw.*(3 个) | 4 | EF Core Sqlite | SQLite 驱动与原生桥接，必需 |
| Microsoft.Extensions.*（Caching/Configuration/DependencyInjection/Logging/Options/Primitives） | 8 | EF Core | EF 的基础设施，必需 |
| ICSharpCode.AvalonEdit | 1 | MdXaml | Markdown 编辑器内核，必需 |
| MdXaml.Plugins | 1 | MdXaml | Markdown 渲染插件机制 |
| CommunityToolkit.Mvvm | 1 | 直接引用 | MVVM 工具 |
| Material3.Core + Material3.Wpf | 2 | 直接引用（新） | M3 主题引擎 + 控件 |
| ~~MaterialDesignThemes.Wpf~~ | ~~1~~ | ~~直接引用~~ | ❌ 已移除（9,686 KB！） |
| ~~MaterialDesignColors~~ | ~~1~~ | ~~直接引用~~ | ❌ 已移除 |
| ~~Microsoft.Xaml.Behaviors~~ | ~~1~~ | ~~MaterialDesign 传递~~ | ❌ 随之消失 |
| ~~System.Reactive~~ | ~~1~~ | ~~XamlFlair 传递~~ | ❌ 随之消失（1,421 KB） |
| ~~Microsoft.Extensions.DependencyModel~~ | ~~1~~ | ~~EF Design 传递~~ | ❌ 随之消失 |

## 三、迁移前后对比

| 指标 | 迁移前 | 迁移后 |
|---|---|---|
| DLL 数量 | 29 | 26 |
| UI 库体积 | MaterialDesign 系 10,137 KB | Material3 系 150 KB（**-10.0 MB**） |
| System.Reactive | 1,421 KB | 无 |
| **输出目录总 DLL 体积** | **≈ 18.4 MB** | **≈ 6.7 MB（-63%）** |

## 四、M3 迁移的实现要点

1. **主题接入**：`App.xaml` 合并 `Material3.Wpf` 的 `Controls.xaml`；`App.OnStartup` 调用
   `M3Theme.Apply(MaterialTheme.FromSeed(#6750A4), isDark: false, Resources)`——
   一个种子色生成全套 M3 角色画刷（Primary/Surface/Outline 等），全 UI 走 `DynamicResource` 可运行时换肤。
   ⚠️ 必须在 `OnStartup`（App.xaml 资源合并之后）调用，放构造函数会被覆盖（踩过坑）。
2. **图标**：M3Icon 是"零内置图标"控件（注册 Path 数据按 Key 引用）。新建 `Services/LpIcons.cs`
   注册了项目实际用到的 **40 个** Pictogrammers MDI 图形（Apache 2.0，键名 = MDI 官方 kebab-case 名）。
   以后要用新图标：去 https://pictogrammers.com/library/mdi/ 找到图形，把 `d` 数据加进 `LpIcons.Glyphs`。
3. **样式键映射**：MaterialDesignFlatButton→`TextButton`、MaterialDesignRaisedButton→`FilledButton`、
   MaterialDesignOutlinedButton→`OutlinedButton`、MaterialDesignCircularProgressBar→`m3:CircularProgress`、
   MaterialDesignDivider→`OutlineVariant`、MaterialDesignCardBackground/Paper→`SurfaceContainer`/`Surface`、
   MaterialDesignBody→`OnSurface`、ValidationErrorBrush→`Error`、PrimaryHueMidBrush→`Primary`。
   HintAssist.Hint → TextBox `Tag`（占位提示视觉暂由 M3 隐式 TextBox 样式接管）。
4. **图标属性类型**：`NavigationItem.IconKind` / `FolderNode.IconKind` 从 `PackIconKind` 枚举改为
   `string`（= LpIcons 键名），XAML 绑定与 C# 赋值同步更新。

## 五、遗留事项

- M3 文本框的浮动占位提示（原 HintAssist）暂以 `Tag` 保留文本，无视觉提示——P5/P7 打磨时统一处理。
- 旧界面（链接/搜索/工具等页）布局仍是 MD2 时代的手写样式，颜色已切到 M3 角色，形状（圆角/药丸）
  待 P5-P7 的 UI 换肤阶段逐页重塑（浏览页 BrowserView 已是 M3 结构，换肤最快）。
